/**
 * [MOD] MonitorPermissionRequest — permission UI for MonitorTool.
 *
 * Placeholder that falls back to the standard permission request UI.
 * In a full implementation, this would show MCP server details and
 * monitoring parameters for the user to approve.
 */

// Re-export null to trigger fallback to FallbackPermissionRequest
export const MonitorPermissionRequest = null
